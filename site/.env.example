# Zoho SMTP credentials for the intake form email function
# Copy to .env.local and fill in real values. NEVER commit .env.local
ZOHO_USER=sender@yourdomain.com
ZOHO_PASS=your-zoho-app-specific-password
CONTACT_TO=notifications@yourdomain.com
